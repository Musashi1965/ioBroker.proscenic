{
  "name": "@iobroker-proscenic/rest-poc",
  "version": "0.0.0",
  "private": true,
  "description": "Private Proscenic legacy REST endpoint discovery proof of concept.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/**/*.test.js",
    "start": "npm run build && node dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=22"
  }
}
